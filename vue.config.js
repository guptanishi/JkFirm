// Vue CLI configuration
// Convert ESLint errors to warnings during development so `npm run serve` doesn't exit
// You can set this back to true to fail on lint errors if desired.
module.exports = {
  lintOnSave: 'warning',
};
